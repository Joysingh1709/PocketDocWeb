import { MessengerAnimeDirective } from './messenger-anime.directive';

describe('MessengerAnimeDirective', () => {
  it('should create an instance', () => {
    const directive = new MessengerAnimeDirective();
    expect(directive).toBeTruthy();
  });
});
